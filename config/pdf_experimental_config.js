/* Stub Cloud — arquivo referenciado pelo index.html; PDF experimental fora do escopo da DEMO. */
(function (global) {
  "use strict";
  global.AURORA_PDF_EXPERIMENTAL_CONFIG = global.AURORA_PDF_EXPERIMENTAL_CONFIG || { enabled: false };
})(window);
